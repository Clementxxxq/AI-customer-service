# 日历功能 - 集成测试指南

## ✅ 测试清单

### 第 1 步：后端单元测试

```bash
# 进入项目目录
cd e:\Learning\AI-customer-service

# 测试可用时间生成
python test_availability.py
```

**预期输出**:
```
============================================================
📅 Testing Availability Service
============================================================

1️⃣ Testing get_available_dates()...
✅ Found 10 available dates
   📆 2026-01-08 (Thursday): 18 slots
      Times: 09:00, 09:30, 10:00...

2️⃣ Testing get_suggested_appointment()...
✅ Suggested appointment: 2026-01-08 at 10:00

3️⃣ Testing JSON response format...
✅ Response structure:
   - available_dates: 3 items
   - suggested: {'date': '2026-01-08', 'time': '10:00'}

4️⃣ Testing edge case (far future dates)...
✅ Far future dates: 714 dates (should be > 100)

============================================================
✅ All tests passed!
============================================================
```

### 第 2 步：后端启动检查

```bash
# 启动后端服务器
cd backend
python main.py
```

**检查点**:
- ✅ 服务器在 http://127.0.0.1:8000 启动
- ✅ 日志显示 `Uvicorn running on ...`
- ✅ 访问 http://127.0.0.1:8000/health 返回 `{"status": "healthy", ...}`

### 第 3 步：后端 API 测试

```bash
# 使用 curl 或 Thunder Client 测试

# 1. 测试简单查询（不返回日历）
POST http://127.0.0.1:8000/api/chat/message
Content-Type: application/json

{
  "content": "你们提供什么服务？",
  "user_id": 1,
  "conversation_id": "test_1"
}

# 预期: 200 OK, availability = null
```

**响应示例**:
```json
{
  "message_id": "msg_xxx",
  "user_message": "你们提供什么服务？",
  "bot_response": "我们提供...",
  "intent": "query",
  "availability": null
}
```

```bash
# 2. 测试预约请求（返回日历）
POST http://127.0.0.1:8000/api/chat/message
Content-Type: application/json

{
  "content": "我想预约洗牙",
  "user_id": 1,
  "conversation_id": "test_2"
}

# 预期: 200 OK, availability 包含时间槽数据
```

**响应示例**:
```json
{
  "message_id": "msg_yyy",
  "user_message": "我想预约洗牙",
  "bot_response": "很好！请选择您喜欢的日期和时间...",
  "intent": "appointment",
  "availability": {
    "available_dates": [
      {
        "date": "2026-01-10",
        "day_of_week": "Friday",
        "slots": ["09:00", "09:30", "10:00", "14:00", "14:30"]
      },
      {
        "date": "2026-01-11",
        "day_of_week": "Saturday",
        "slots": ["10:00", "11:00", "15:00"]
      }
    ],
    "suggested": {
      "date": "2026-01-10",
      "time": "10:00"
    }
  }
}
```

### 第 4 步：前端依赖安装

```bash
# 安装新的日历依赖
cd frontend
npm install react-day-picker date-fns
```

**检查**:
- ✅ `package-lock.json` 更新
- ✅ `node_modules` 包含新依赖
- ✅ 没有错误或警告

### 第 5 步：前端启动

```bash
# 启动开发服务器
npm run dev
```

**检查点**:
- ✅ 服务器在 http://localhost:3000 启动
- ✅ 浏览器打开无错误
- ✅ 聊天界面正常加载

### 第 6 步：前端集成测试

#### 测试场景 1: 查询（无日历）

1. 打开 http://localhost:3000
2. 输入: "你们提供什么服务？"
3. **验证**:
   - ✅ AI 回复服务列表
   - ✅ 没有显示日历组件
   - ✅ 只显示文本消息

#### 测试场景 2: 预约请求（显示日历）

1. 输入: "我想预约洗牙"
2. **验证**:
   - ✅ AI 返回预约消息
   - ✅ **显示日历组件**
   - ✅ 看到日期列表（Jan 10, 11, 13...）
   - ✅ 每个日期显示可用槽位数
   - ✅ 没有显示周末日期
   - ✅ 显示推荐时间提示

#### 测试场景 3: 日期选择

1. 点击第一个日期 (例如 Jan 10)
2. **验证**:
   - ✅ 日期按钮变蓝（高亮）
   - ✅ 下方显示时间选择按钮
   - ✅ 显示该日期的所有可用时间 (09:00, 09:30, 10:00...)

#### 测试场景 4: 时间选择

1. 点击时间按钮 (例如 10:00)
2. **验证**:
   - ✅ 前端发送消息: "I'd like to book at 10:00 on 2026-01-10"
   - ✅ 消息出现在聊天中
   - ✅ 后端处理并返回预约确认

#### 测试场景 5: 预约确认

1. **验证**:
   - ✅ AI 返回: "✅ 预约成功..."
   - ✅ 显示预约 ID 和时间
   - ✅ 日历消失
   - ✅ 对话继续

## 🔍 调试技巧

### 浏览器开发者工具

```javascript
// F12 打开开发者工具 → Console

// 检查网络请求
// Network 标签 → 查看 POST /api/chat/message 请求/响应

// 查看响应格式
// 在 Network 标签中点击请求 → Response 标签
```

### 后端日志调试

```bash
# 查看后端日志
# 在后端终端中看到日志，例如:
# INFO:     127.0.0.1:54321 - "POST /api/chat/message HTTP/1.1" 200 OK
```

### 前端控制台日志

```javascript
// 在 CalendarPicker.tsx 中添加日志
console.log('Available dates:', availableDates);
console.log('Selected date:', selectedDate);
console.log('Selected time:', time);
```

## 🧪 边界情况测试

### 1. 没有可用时间的医生

```bash
POST /api/chat/message
{
  "content": "我想和 Dr. Smith 预约（不存在的医生）"
}

# 预期: 医生不存在错误
```

### 2. 所有时间已满

如果数据库中的某个日期所有时间槽都已被预约：
- ✅ 该日期应该不显示
- ✅ 或该日期的 slots 数组为空

### 3. 超长对话

```bash
# 进行多轮对话，验证对话历史保持
1. "我想预约"
2. "洗牙"
3. 选择日期时间
4. 确认预约

# 预期: 整个对话流畅完整
```

### 4. 多个浏览器标签

- 打开 2 个标签页
- 各自进行独立对话
- **验证**: 对话相互独立，不混淆

## 📊 性能测试

```bash
# 1. 响应时间测试
# 在 Network 标签中查看请求时间
# 预期: < 1 秒

# 2. 内存使用
# 打开 DevTools → Memory → 拍摄快照
# 预期: 前端 < 50MB, 后端合理增长

# 3. 多个预约请求
# 快速发送多个预约请求
# 预期: 服务器不崩溃，正常处理
```

## ✨ 完整端到端流程检查

```
┌─────────────────────────────────────────┐
│ 1. 后端服务启动                          │
│    python main.py                       │
│    ✅ 端口 8000 监听                    │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ 2. 前端服务启动                          │
│    npm run dev                          │
│    ✅ 端口 3000 监听                    │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ 3. 打开浏览器                            │
│    http://localhost:3000                │
│    ✅ 聊天界面加载                      │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ 4. 用户输入预约请求                      │
│    "我想预约洗牙"                       │
│    ✅ 消息发送                          │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ 5. 后端处理                              │
│    ✅ NLU 识别意图                      │
│    ✅ AvailabilityService 生成时间      │
│    ✅ 返回 JSON 响应                    │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ 6. 前端显示日历                          │
│    ✅ CalendarPicker 渲染                │
│    ✅ 显示所有可用日期                   │
│    ✅ 显示推荐时间                       │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ 7. 用户选择日期时间                      │
│    点击: 2026-01-10 at 10:00            │
│    ✅ 转换为自然语言                    │
│    ✅ 发送确认消息                       │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ 8. 后端确认预约                          │
│    ✅ 执行预约逻辑                      │
│    ✅ 保存到数据库                      │
│    ✅ 返回成功消息                      │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ ✅ 预约完成！                            │
│ "您的预约时间是: 2026-01-10 10:00"      │
└─────────────────────────────────────────┘
```

## 🐛 常见问题排查

### 问题 1: 日历不显示

**可能原因**:
- [ ] 后端没有返回 `availability` 字段
- [ ] 前端 JSON 解析失败
- [ ] CalendarPicker 组件未导入

**解决**:
1. 检查浏览器 DevTools → Network → 查看响应 JSON
2. 检查浏览器 Console 是否有错误
3. 检查 DentalChat.tsx 的导入语句

### 问题 2: 点击时间无反应

**可能原因**:
- [ ] `onSelectDateTime` 回调未定义
- [ ] `handleSelectDateTime` 函数有错误

**解决**:
1. 在 CalendarPicker 中添加 console.log
2. 检查回调函数是否正确传递

### 问题 3: 后端返回错误

**可能原因**:
- [ ] 数据库连接失败
- [ ] Llama 模型不可用

**解决**:
1. 查看后端日志
2. 检查数据库状态
3. 运行 test_availability.py 检查核心功能

## 📝 测试记录模板

```markdown
## 测试日期: 2026-01-07

### 后端测试
- [ ] test_availability.py 通过
- [ ] 后端启动成功
- [ ] API 返回正确 JSON

### 前端测试
- [ ] npm install 成功
- [ ] 前端启动成功
- [ ] 加载页面无错误

### 集成测试
- [ ] 查询请求正常
- [ ] 预约请求返回日历
- [ ] 日历显示正确
- [ ] 选择日期时间成功
- [ ] 预约完成

### 边界测试
- [ ] 多标签页独立
- [ ] 快速请求处理
- [ ] 无可用时间处理

### 问题记录
（如有问题，记录在此）

### 签名
测试人员: ___________
日期: ___________
```

---

**提示**: 按顺序完成所有测试步骤，确保整个系统正常运行！✅
